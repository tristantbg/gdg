section.section.section--exhibitions
	.exhibition-link
		&:not(:last-child)
			padding-bottom 6rem
			margin-bottom 1.5rem
			border-bottom 1px solid

section.section.section--exhibitions-grid
	.exhibition-link
		.responsive-image
			// border 1px solid #979797
			.ph
				// margin-bottom 1px
		.lead
			+below($sm)
				font-size 1.8rem
