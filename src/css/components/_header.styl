header
	hideselect()
	position fixed
	top 0
	left 0
	width 100%
	height 8rem
	display flex
	align-items flex-start
	justify-content center
	background-image linear-gradient(to bottom, $menu-color 50%, rgba(255, 255, 255, 0));
	z-index 200
	#site-title
		site-title()
		font-size 2rem
		padding $padding 0
